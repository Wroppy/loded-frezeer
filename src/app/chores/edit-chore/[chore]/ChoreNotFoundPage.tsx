import { Card } from "@mantine/core";
import React from "react";

type Props = { id: string };

const ChoreNotFoundPage = ({ id }: Props) => {
  return <Card>ChoreNotFoundPage</Card>;
};

export default ChoreNotFoundPage;