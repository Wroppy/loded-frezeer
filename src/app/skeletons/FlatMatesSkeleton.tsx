import { Skeleton } from "@mantine/core";

import React from "react";

type Props = {};

const FlatMatesSkeleton = (props: Props) => {
  return <Skeleton height={432} width={600}/>;
};

export default FlatMatesSkeleton;
